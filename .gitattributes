# Auto detect text files and perform LF normalization
* text=auto

# Force LF line endings for shell scripts
*.sh text eol=lf

# Force CRLF line endings for batch files (if any remain)
*.bat text eol=crlf

# Binary files
*.db binary
*.db-shm binary
*.db-wal binary
